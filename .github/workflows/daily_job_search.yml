name: Daily Job Search

on:
  schedule:
    # 07:00 UTC = 10:00 Kyiv (літній час, UTC+3) / 09:00 Kyiv (зимовий час, UTC+2)
    # Тобто запуск має потрапляти в межі вікна 9:00-10:00 за київським часом.
    #
    # ВАЖЛИВО: запуск навмисно НЕ на рівно "0" хвилині. GitHub Actions
    # отримує величезний потік запланованих завдань саме на початку кожної
    # години з усіх репозиторіїв світу одночасно, тому запуски "на рівну
    # годину" часто стають в чергу і затримуються на кілька годин (саме
    # це й спричиняло звіти о 14:55-15:00 замість ранку). Хвилина "13"
    # обрана довільно, аби уникнути цього піку навантаження.
    - cron: "13 7 * * *"
  workflow_dispatch: {}   # дозволяє запустити вручну з вкладки Actions для тесту

permissions:
  contents: write   # потрібно, щоб закомітити оновлений data/seen_jobs.json

jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run job search agent
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python src/main.py

      - name: Commit updated seen_jobs.json
        run: |
          git config user.name "job-search-agent-bot"
          git config user.email "actions@github.com"
          git add data/seen_jobs.json
          git diff --staged --quiet || git commit -m "chore: update seen jobs [skip ci]"
          git push
