"""
Indeed офіційно закрив публічний RSS кілька років тому в більшості країн
і активно блокує прямий HTML-скрапінг через Cloudflare. Робочого
100%-безкоштовного шляху без офіційного Indeed Publisher API (платний
доступ для великих партнерів) наразі немає.

Це джерело навмисно повертає порожній список і не ламає весь пайплайн.
Рекомендована заміна: налаштуй безкоштовний "Job Alert" (email-розсилку)
прямо в акаунті Indeed на ключові слова зі списку — це офіційний і
безкоштовний канал, який агент не дублює.
"""
from .base import logger


def search(keywords: list) -> list:
    logger.info("Indeed: скрапінг вимкнено (немає безкоштовного стабільного шляху), пропускаємо.")
    return []
